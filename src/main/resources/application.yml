spring:
  application:
    name: gelir-gider-takip
  
  # PostgreSQL Configuration
  datasource:
    url: jdbc:postgresql://185.136.206.32:5432/gelir_gider_db
    username: gelir_user
    password: gelir_sifre_123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
  
  # JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        use_sql_comments: false
    open-in-view: false
  
  # Redis Configuration
  data:
    redis:
      host: 185.136.206.32
      port: 6379
      password: 
      timeout: 60000
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  
  # Thymeleaf Configuration
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

# JWT Configuration
jwt:
  secret: YourSecretKeyHerePleaseChangeThisToAVeryLongAndSecureRandomString123456789
  expiration: 3600000 # 1 hour in milliseconds

# Server Configuration
server:
  port: 1818
  servlet:
    context-path: /
    session:
      timeout: 30m

# Logging Configuration
logging:
  level:
    root: INFO
    com.gelir.gider: INFO
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Application Configuration
app:
  admin:
    username: admin
    password: admin123
    email: admin@gelir-gider.com
